<h1>Trips#show</h1>

<%= @trip.name %>
<%= @trip.destination %>

<div>
  <% if @trip.photos.attached? %>
    <% @trip.photos.each do |photo| %>
      <%= cl_image_tag photo.key, height: 200, crop: :fill %>
    <% end %>
  <% end %>
</div>
