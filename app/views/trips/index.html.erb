<h1>Trips</h1>
<div class="row">
  <% @trips.each do |trip| %>
    <div class="col-lg-3">
      <div class="card">
        <%= link_to trip.name, trip_path(trip) %>
        <%= trip.destination %>
      </div>
    </div>
  <% end %>
</div>
